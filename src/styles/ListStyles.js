import styled from "styled-components";
import { Button, TextField } from "@mui/material";

export const OwnTextField = styled(TextField)({
    ":focus": {
        secondary: "black"
    }
    
})

export const OwnButton = styled(Button)({
})
